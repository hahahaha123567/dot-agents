# Engineering AGENTS

## Scope

- 本文件维护用户级工程代理规则, 适用于本机任意位置的开发、修改、重构、排障与评审
- 本文件不是当前 `.agents` 仓库说明, 不写本仓库目录地图、构建命令或模块导览
- 这里只保留长期有效、跨项目通用, 且不能仅靠代码推断的用户规则
- 更近层级的 `AGENTS.md` 可补充或收紧本文件
- 编程语言无关原则适用于所有工程项目; Java / Spring 规则仅在对应技术栈项目中适用

## 编程语言无关原则

### 定位与上下文

- 代码仓库是唯一记录系统; 影响开发、评审、排障、交付的知识必须落到 repo 内
- 能用 linter、结构测试、静态检查、CI 强制的规则, 不只写成自然语言
- 跨项目后端微服务代码优先在本地 `~/IdeaProjects/` 目录下定位
- 编码前先读 manifest、配置、包结构、已有实现和测试, 再决定改法
- 修改前优先读 exports、调用方、共享工具和相邻实现, 避免重复造轮子
- 匹配项目当前语言版本、框架主版本、持久化方案、日志框架、测试框架和命名习惯
- 讨论结论若影响代码或协作, 必须写入 repo; 持续性任务应有 versioned 计划或进度记录
- 卡住时先补上下文、工具、脚本、约束或文档, 不靠重复硬试推进

### 工作方式

- 只做被明确要求或明显必要的修改, 每一行改动都应能追溯到用户目标
- 默认最小充分修改, 不顺手重构、格式化、升级、清理历史死代码或添加未来弹性
- 不给未改动代码添加 docstring、注释、类型标注等顺手改进
- 只有逻辑不直观时才加注释; 不为自解释代码添加说明
- 不为一次性操作创建 helper、工具函数或抽象层; 三行相似代码通常优于过早抽象
- 不为了假想未来需求设计; 合适复杂度是当前任务所需的最低复杂度
- 不使用向后兼容式 hack, 例如保留无用重导出、重命名 `_vars` 或给已删除代码加 `// removed`
- 非琐碎任务若存在多个合理解释且影响范围, 先说明假设、权衡或澄清
- 代码库出现互斥模式时, 明确选择一种并说明原因, 不折中混用
- 多步任务在关键 checkpoint 后确认状态和产物, 再继续推进
- Plan 模式下必须优先使用中文输出, 包括计划、假设、问题澄清、权衡说明、检查清单、阶段性确认和报告
- Plan 模式只有在引用代码标识符、命令、错误原文、第三方 API 名称或用户明确要求英文时, 才保留必要英文
- 模型适合判断、分类、草稿、总结和抽取; 路由、重试、状态码处理、确定性转换交给代码表达

### 工程边界

- 边界层只做协议适配; 应用或服务层负责业务编排; 数据访问层只负责数据访问
- 外部系统调用放在清晰的 Client、Remote 或 Gateway 边界内
- 配置、拦截器、监听器、任务和消费者应独立放在清晰模块或包下
- 请求对象、响应对象、持久化对象和外部系统 DTO 应分离
- 不把外部系统 DTO 直接穿透到核心业务层
- 字段命名必须表达业务语义, 禁止含义模糊的 `flag`、`type1`、`ext`、`data`
- 对外接口字段一旦发布, 默认按兼容接口处理, 避免随意改名或改变语义

### 可靠性

- 只在系统边界做输入校验, 包括用户输入、外部 API、消息入口或跨系统调用
- 业务异常与系统异常分开处理, 对外服务优先提供统一异常出口
- 不吞异常, 不静默返回默认成功, 不写无上下文的宽泛异常捕获
- 异常信息要么转换为可读业务错误, 要么保留上下文后继续抛出
- 部分成功、跳过记录、无法确认的状态必须明确返回、记录或上报
- 事务只放在明确服务边界, 只在需要一致性时开启; 事务中避免远程调用、消息投递和长耗时 IO
- 跨系统一致性优先考虑幂等、补偿、重试策略和事件最终一致性
- 远程调用必须显式考虑超时、重试、降级、幂等和错误码处理; 不把下游失败伪装成成功
- MQ 消费、定时任务和批处理必须考虑重复执行、断点续跑、失败重试、补偿和可观测性
- 异步执行时保留必要上下文; 线程池要显式命名、容量清晰、拒绝策略明确
- 共享状态必须明确同步策略; 并发优化先保证正确性, 再追求吞吐

### 数据、配置与日志

- SQL 参数必须安全绑定, 禁止拼接用户输入
- 查询默认写明确列名, 避免新增 `SELECT *`; 排序、分页、过滤条件必须显式
- 复杂查询优先保证可读性和可验证性, 复杂映射、复杂 SQL、新增索引敏感查询应补测试或最少验证用例
- 环境差异放在配置文件或环境变量中; 禁止硬编码密钥、地址、凭证和环境开关
- 超时、重试、线程池、开关、队列大小、阈值都应可配置
- 新增配置要说明默认值策略和缺省行为; 新增 Apollo 配置还要给出配置示例、空配置行为和日志表现
- 日志使用参数化写法, 带足够业务上下文, 如 `traceId`、`requestId`、`tenantId`、`taskId`、`orderId`、`logId`
- 不输出手机号、身份证号、token、密钥、密码、完整隐私文本等敏感信息原文
- `error` 日志要包含失败原因和关键上下文, 不只打 `"error"`
- 高频循环日志默认降级到 `debug` 或抽样
- 关键业务分支必须有可检索日志, 尤其是配置为空、锁获取失败、下游异常、MQ 重复消费、算法异常返回

### 测试、评审与验证

- 新增复杂业务逻辑默认补测试, 覆盖正常路径、参数校验失败、下游异常或超时、幂等或重复执行
- 优先快速、稳定、可重复运行的测试; 能写单元测试时, 不滥用全量集成测试
- 测试必须验证业务意图, 不只验证表面行为
- 修改代码后优先运行最贴近改动面的测试、编译或静态检查
- 修 bug 时优先补能复现问题的测试或最小验证步骤, 再修到通过
- 评审优先看行为回归、兼容性破坏、未经验证假设、无关改动、风格漂移和不必要复杂度
- 重点检查失败路径、边界条件、幂等、事务边界、异常吞没、伪成功返回、日志上下文、配置说明和测试缺口
- 无法运行验证时, 明确说明未验证原因、剩余风险和建议命令

## Java / Spring 相关规则

### 项目上下文

- Java 项目编码前优先查看 `pom.xml`、`build.gradle*`、`src/main/resources/application*.yml`、`application*.properties`、包结构和测试写法
- 先遵守项目当前 Java 版本、Spring Boot 主版本、持久化框架、日志框架、测试框架和命名习惯
- 未被任务要求时, 不主动升级大版本依赖或框架

### 依赖注入与 Bean

- 新代码默认使用构造器注入, 推荐 `final` 字段加构造器
- 禁止新增字段注入, 例如 `@Autowired private XxxService xxxService;`
- 必选依赖通过构造器传入, 可选依赖才允许 setter 或配置注入
- Bean 尽量保持无状态; 依赖过多时优先拆分职责
- 项目已使用 Lombok 时, 可用 `@RequiredArgsConstructor`

### 分层与模型

- Controller 负责协议适配, 不承载核心业务; 入参、出参不要直接暴露数据库实体
- Service 或 Application 层负责业务编排; Repository、Mapper、DAO 层负责数据访问
- Client、Remote、Gateway 层负责调用外部系统
- Config、Interceptor、Listener、Job、Consumer 应独立放在清晰包下
- 不把业务决策塞进 Controller、Mapper 或 Repository

### 校验、异常与事务

- 边界层优先使用 `@Validated`、`@Valid` 和 Bean Validation
- 对外服务优先提供统一异常出口, 例如 `@ControllerAdvice`
- 禁止无上下文 `catch (Exception e)` 后静默返回默认成功
- `@Transactional` 只放在明确服务边界, 不放在 Controller、工具类、Mapper 上
- 只有需要一致性时才开启事务, 事务中避免远程调用、MQ 投递、长耗时 IO

### 持久化与配置

- MyBatis、MyBatis-Plus、JPA 或其他持久化层都必须安全绑定 SQL 参数, 禁止拼接用户输入
- 查询默认写明确列名, 避免新增 `SELECT *`; 排序、分页、过滤条件必须显式
- 持久化层只负责数据访问, 复杂映射、复杂 SQL、新增索引敏感查询应补测试或最少验证用例
- 成组配置优先使用 `@ConfigurationProperties`, 不要无节制堆 `@Value`

### Lombok、版本与注释

- 慎用 `@Data`; 对参与比较、去重、缓存键、Map 键的对象, 显式确认 `equals/hashCode` 语义
- 更推荐按需使用 `@Getter`、`@Setter`、`@ToString`、`@EqualsAndHashCode`、`@RequiredArgsConstructor`
- 修改代码时先以当前项目版本边界为准; Java 8 项目不要使用高版本语法
- Spring Boot 2.x 不混入 Spring Boot 3.x / `jakarta.*`; Spring Boot 3.x 不回退到旧 `javax.*`
- 生成代码注释时句末不要加句号; 复杂函数必须增加简明中文注释说明核心意图或关键分支
- 所有单元测试函数必须增加简明中文注释, 说明被验证的业务场景或失败路径
- 官方资料按需参考 Spring、MyBatis、SLF4J 等项目文档, 但以当前项目版本边界为准

## 一句话标准

- 兼容当前项目版本边界, 保持分层清楚和模型隔离
- 不新增字段注入, 不吞异常, 不拼 SQL, 不拼日志
- 事务、远程调用、MQ、定时任务先考虑失败路径和幂等
- 配置、日志、并发和外部依赖必须可运维、可追踪、可验证
- 新增复杂逻辑默认补测试, 修改后运行最贴近改动面的验证
